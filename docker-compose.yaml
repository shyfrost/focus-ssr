version: "3"

services:
  app:
    image: shyfrost/focus-ssr-app
    expose:
      - 7000

  nginx:
    image: shyfrost/nginx-ssr
    restart: always
    ports:
      - 80:80

  postgres:
    image: postgres:13.8-alpine
    expose:
     - 5432
    env_file:
     - ./.env.db

  redis:
    image: redis:alpine
    expose:
      - 6379